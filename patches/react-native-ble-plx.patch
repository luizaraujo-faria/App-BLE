diff --git a/android/src/main/java/com/bleplx/BlePlxModule.java b/android/src/main/java/com/bleplx/BlePlxModule.java
index c73164058dd5eded7ecfc37aed6d7ef068695e74..26226bf1d9c207cf0aa6a779b617a1cb085d771d 100644
--- a/android/src/main/java/com/bleplx/BlePlxModule.java
+++ b/android/src/main/java/com/bleplx/BlePlxModule.java
@@ -114,8 +114,9 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
   }
 
   @ReactMethod
-  public void destroyClient(final Promise promise) {
-    if (!this.isRequestPossibleHandler("destroyClient", promise)) {
+  public synchronized void destroyClient(final Promise promise) {
+    if (bleAdapter == null) {
+      promise.resolve(null);
       return;
     }
 
@@ -124,6 +125,7 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
     promise.resolve(null);
   }
 
+
   // Mark: Common --------------------------------------------------------------------------------
 
   @ReactMethod
@@ -168,7 +170,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
     }, new OnErrorCallback() {
       @Override
       public void onError(BleError error) {
-        safePromise.reject(null, errorConverter.toJs(error));
+        safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
       }
     });
   }
@@ -187,7 +192,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
     }, new OnErrorCallback() {
       @Override
       public void onError(BleError error) {
-        safePromise.reject(null, errorConverter.toJs(error));
+        safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
       }
     });
   }
@@ -318,7 +326,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       });
   }
@@ -338,7 +349,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       });
   }
@@ -358,7 +372,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       });
   }
@@ -395,11 +412,13 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
     }
     bleAdapter.connectToDevice(
       deviceId,
-      new ConnectionOptions(autoConnect,
+      new ConnectionOptions(
+        autoConnect,
         requestMtu,
-        refreshGattMoment,
-        timeout != null ? timeout.longValue() : null,
-        connectionPriority),
+        refreshGattMoment != null ? refreshGattMoment : RefreshGattMoment.NEVER,
+        timeout != null ? timeout.longValue() : 10000L,
+        connectionPriority
+      ),
       new OnSuccessCallback<Device>() {
         @Override
         public void onSuccess(Device data) {
@@ -422,7 +441,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       });
   }
@@ -442,7 +464,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       });
   }
@@ -483,7 +508,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       });
   }
@@ -627,7 +655,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       }
     );
@@ -654,7 +685,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       }
     );
@@ -680,7 +714,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       });
   }
@@ -706,7 +743,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       }
     );
@@ -732,7 +772,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       }
     );
@@ -757,7 +800,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       }
     );
@@ -788,7 +834,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       }
     );
@@ -818,7 +867,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       }
     );
@@ -848,7 +900,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError error) {
-          safePromise.reject(null, errorConverter.toJs(error));
+          safePromise.reject(
+  error.errorCode != null ? error.errorCode.name() : "UNKNOWN_ERROR",
+  errorConverter.toJs(error)
+);
         }
       }
     );
@@ -878,7 +933,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       }, new OnErrorCallback() {
         @Override
         public void onError(BleError bleError) {
-          promise.reject(null, errorConverter.toJs(bleError));
+          promise.reject(
+            bleError.errorCode != null ? bleError.errorCode.name() : "UNKNOWN_ERROR",
+            errorConverter.toJs(bleError)
+          );
         }
       });
   }
@@ -906,7 +964,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       new OnErrorCallback() {
         @Override
         public void onError(BleError bleError) {
-          promise.reject(null, errorConverter.toJs(bleError));
+          promise.reject(
+            bleError.errorCode != null ? bleError.errorCode.name() : "UNKNOWN_ERROR",
+            errorConverter.toJs(bleError)
+          );
         }
       });
   }
@@ -932,7 +993,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       new OnErrorCallback() {
         @Override
         public void onError(BleError bleError) {
-          promise.reject(null, errorConverter.toJs(bleError));
+          promise.reject(
+            bleError.errorCode != null ? bleError.errorCode.name() : "UNKNOWN_ERROR",
+            errorConverter.toJs(bleError)
+          );
         }
       });
   }
@@ -956,7 +1020,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       new OnErrorCallback() {
         @Override
         public void onError(BleError bleError) {
-          promise.reject(null, errorConverter.toJs(bleError));
+          promise.reject(
+            bleError.errorCode != null ? bleError.errorCode.name() : "UNKNOWN_ERROR",
+            errorConverter.toJs(bleError)
+          );
         }
       });
   }
@@ -988,7 +1055,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       new OnErrorCallback() {
         @Override
         public void onError(BleError bleError) {
-          promise.reject(null, errorConverter.toJs(bleError));
+          promise.reject(
+            bleError.errorCode != null ? bleError.errorCode.name() : "UNKNOWN_ERROR",
+            errorConverter.toJs(bleError)
+          );
         }
       }
     );
@@ -1019,7 +1089,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       new OnErrorCallback() {
         @Override
         public void onError(BleError bleError) {
-          promise.reject(null, errorConverter.toJs(bleError));
+          promise.reject(
+            bleError.errorCode != null ? bleError.errorCode.name() : "UNKNOWN_ERROR",
+            errorConverter.toJs(bleError)
+          );
         }
       }
     );
@@ -1048,7 +1121,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       new OnErrorCallback() {
         @Override
         public void onError(BleError bleError) {
-          promise.reject(null, errorConverter.toJs(bleError));
+          promise.reject(
+            bleError.errorCode != null ? bleError.errorCode.name() : "UNKNOWN_ERROR",
+            errorConverter.toJs(bleError)
+          );
         }
       }
     );
@@ -1075,7 +1151,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
       new OnErrorCallback() {
         @Override
         public void onError(BleError bleError) {
-          promise.reject(null, errorConverter.toJs(bleError));
+          promise.reject(
+            bleError.errorCode != null ? bleError.errorCode.name() : "UNKNOWN_ERROR",
+            errorConverter.toJs(bleError)
+          );
         }
       }
     );
@@ -1092,6 +1171,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
   }
 
   private void sendEvent(@NonNull Event event, @Nullable Object params) {
+    if (!reactContext.hasActiveCatalystInstance()) {
+      return;
+    }
+
     getReactApplicationContext()
       .getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter.class)
       .emit(event.name, params);
@@ -1101,7 +1184,10 @@ public class BlePlxModule extends ReactContextBaseJavaModule {
     if(this.bleAdapter == null){
       BleError bleError = new BleError(BleErrorCode.BluetoothManagerDestroyed, String.format("BleManager cannot call the %s function because BleManager has been destroyed", functionName), null);
 
-      promise.reject(null, errorConverter.toJs(bleError));
+      promise.reject(
+        bleError.errorCode != null ? bleError.errorCode.name() : "UNKNOWN_ERROR",
+        errorConverter.toJs(bleError)
+      );
       return false;
     }
 
